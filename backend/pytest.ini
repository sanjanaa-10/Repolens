[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    integration: requires network access to GitHub (skipped by default)
    network: requires live network access (skipped by default)
addopts = -m "not integration and not network"